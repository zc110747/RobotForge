# =============================================================================
# RobotForge v0.1 —— 依赖镜像（给 `pip install -r requirements.txt` 用）
# -----------------------------------------------------------------------------
# ★ 真值源是 pyproject.toml。本文件是它的**扁平镜像**，两者必须一致。
# ★ 只用 `>=` 下界，不锁精确版本：
#   本项目的验收判据落在外层协议（RobotModel / FK 往返 / WS 帧），
#   不落在某个依赖的补丁号上。锁死版本只会让换环境时更难装。
# =============================================================================

# --- Web 层（§45 / §52）---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7

# --- 数值与解析 ---
numpy>=1.26
PyYAML>=6.0

# --- 物理后端（§60-§63）---
mujoco>=3.1

# --- 测试（§67）---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
websockets>=12.0
